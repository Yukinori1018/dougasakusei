# フォント

サムネ生成で使用するフォントを配置します。`.gitignore` で実ファイルは除外しています。

## 推奨
- **Noto Sans JP Bold** (SIL OFL 1.1, 商用 OK)
  - https://fonts.google.com/noto/specimen/Noto+Sans+JP
  - DL 後 `NotoSansJP-Bold.ttf` として配置

## 設定
`config.toml` の `[thumbnail].font_path` でパスを指定。
